{
  "builds": [
    { "src": "frontend/batalha-naval/package.json", "use": "@vercel/next" },
    { "src": "URLs.py", "use": "@vercel/python" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "URLs:app" },  
    { "src": "/(.*)", "dest": "frontend/batalha-naval/.next/$1" } 
  ]
}
